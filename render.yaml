services:
  - type: web
    name: birr-tracker
    env: node
    buildCommand: "npm install && npm run build && npm run build:server"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: NODE_ENV
        value: production
